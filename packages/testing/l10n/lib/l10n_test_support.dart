/// Shared ARB coverage verification for the localization convention
/// established by issue #33.
///
/// Pure Dart (dart:io + dart:convert) so it runs under both `dart test`
/// and `flutter test`. See the README for the policy this enforces.
library;

export 'src/arb_coverage.dart';
