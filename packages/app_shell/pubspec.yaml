name: app_shell
description: "Shared application shell: bootstrap sequencing, routing, and shell screens"
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.13.0
  flutter: ">=3.47.1"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  auth:
    path: ../features/auth
  di:
    path: ../core/di
  feedback:
    path: ../features/feedback
  household:
    path: ../features/household
  interfaces:
    path: ../core/interfaces
  models:
    path: ../core/models
  network_interface:
    path: ../network/network_interface
  observability:
    path: ../core/observability
  server_onboarding:
    path: ../features/server_onboarding
  ui:
    path: ../ui/widgets
  ui_tokens:
    path: ../ui/tokens

  # State management
  flutter_bloc: ^9.1.1
  hydrated_bloc: ^11.0.0
  logging: ^1.3.0

  # Navigation
  go_router: ^18.0.0

  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^10.0.0
  mocktail: ^1.0.5
  flutter_lints: ^6.0.0
  very_good_analysis: ^10.2.0
  l10n_test_support:
    path: ../testing/l10n

flutter:
  generate: true
  uses-material-design: true
