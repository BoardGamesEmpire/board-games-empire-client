name: di
description: "Dependency injection setup and configuration"
publish_to: none

environment:
  sdk: ^3.13.0
  flutter: ">=3.47.1"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  interfaces:
    path: ../interfaces
  models:
    path: ../models

  pub_semver: ^2.2.0

  # DI
  get_it: ^9.2.1
  injectable: ^3.0.0

dev_dependencies:
  build_runner: ^2.16.0
  injectable_generator: ^3.1.3
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.2.0
  mocktail: ^1.0.5
