# 0.0.1

- Initial token layer (#32): `BgeColorSchemes` (light / dark / high-contrast
  light / high-contrast dark, WCAG 2.1 AA-verified), `BgeTypography` scale,
  `BgeTokens` `ThemeExtension` (spacing, radii, motion, tap-target,
  focus-outline), `BgeTheme` factories with the theme-level accessibility
  baseline, `BgeTextScale` clamp, `BgeMotion` reduced-motion helpers, and
  WCAG contrast utilities.
