<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Backup posture (#16, encryption-at-rest):
         The local databases are encrypted with keys held in the Android
         Keystore, which never leave this device. A backed-up database is
         therefore undecryptable after restore — backups buy nothing and
         manufacture the key-loss scenario. allowBackup=false disables
         cloud backup; dataExtractionRules additionally opts out of
         Android 12+ cloud backup and device-to-device transfer. -->
    <application
        android:label="Board Games Empire"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Deep links (#10): Flutter's built-in deep linking (default-on
                 since Flutter 3.24) would push raw bge:// URLs straight into
                 the Navigator, bypassing our normalization/validation
                 pipeline and double-handling links alongside app_links.
                 Explicitly disabled per the app_links setup docs. -->
            <meta-data
                android:name="flutter_deeplinking_enabled"
                android:value="false" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Deep links (#10): receive bge://server/{serverId}/... custom-
                 scheme URLs, delivered to app_links via onNewIntent
                 (singleTop) or the launch intent (cold start). autoVerify is
                 deliberately absent — verified App Links require a per-server
                 .well-known/assetlinks.json a self-hosted product cannot
                 reliably publish; custom scheme only (#10 decision). The
                 host filter narrows delivery to our published authority. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="bge" android:host="server"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
