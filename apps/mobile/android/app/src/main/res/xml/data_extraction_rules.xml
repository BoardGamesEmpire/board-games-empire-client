<?xml version="1.0" encoding="utf-8"?>
<!-- Excludes all app data from Android 12+ cloud backup and
     device-to-device transfer. See AndroidManifest.xml for rationale
     (#16, encryption-at-rest): database encryption keys are
     Keystore-bound and cannot migrate, so transferred data would be
     undecryptable; the server is the source of truth and re-sync
     rebuilds local state on a new device. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
